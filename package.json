{
  "name": "winccoa-vscode-logviewer",
  "displayName": "WinCC OA LogViewer",
  "description": "LogViewer for WinCC OA - View and analyze log files in real-time",
  "publisher": "RichardJanisch",
  "version": "2.1.0",
  "author": "RichardJanisch",
  "license": "MIT",
  "icon": "resources/wincc_oa_logviewer_512.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/winccoa-tools-pack/vscode-winccoa-logviewer"
  },
  "engines": {
    "vscode": "^1.107.1"
  },
  "categories": [
    "Other",
    "Visualization"
  ],
  "extensionDependencies": [
    "RichardJanisch.winccoa-project-admin"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "winccoa-logviewer.open",
        "title": "Open WinCC OA LogViewer",
        "category": "WinCC OA"
      }
    ],
    "languageModelTools": [
      {
        "name": "logviewer_query_logs",
        "displayName": "Query WinCC OA Logs",
        "modelDescription": "Query log events with filters (severity, time range, search pattern). Returns recent log events matching the criteria. Use when the user asks about logs, errors, warnings, or specific events in WinCC OA.",
        "canBeReferencedInPrompt": true,
        "toolReferenceName": "winccoa_logs",
        "icon": "$(search)",
        "userDescription": "Query and filter WinCC OA log events",
        "inputSchema": {
          "type": "object",
          "properties": {
            "severity": {
              "type": ["string", "array"],
              "description": "Filter by severity: 'INFO', 'WARNING', 'FATAL', 'SEVERE', 'DEBUG', or array of severities",
              "items": {
                "type": "string",
                "enum": ["INFO", "WARNING", "FATAL", "SEVERE", "DEBUG", "OTHER"]
              }
            },
            "searchPattern": {
              "type": "string",
              "description": "Search pattern to match in message, scope, or identifier (case-insensitive)"
            },
            "lastMinutes": {
              "type": "number",
              "description": "Only show events from the last N minutes"
            },
            "limit": {
              "type": "number",
              "description": "Maximum number of results to return (default: 100)"
            }
          }
        }
      },
      {
        "name": "logviewer_get_recent_errors",
        "displayName": "Get Recent WinCC OA Errors",
        "modelDescription": "Get the last N error/warning events from WinCC OA logs. Use when the user asks about recent errors, problems, or issues in the system.",
        "canBeReferencedInPrompt": true,
        "toolReferenceName": "winccoa_errors",
        "icon": "$(error)",
        "userDescription": "Get recent errors and warnings from WinCC OA",
        "inputSchema": {
          "type": "object",
          "properties": {
            "limit": {
              "type": "number",
              "description": "Number of recent errors to return (default: 10)"
            }
          }
        }
      },
      {
        "name": "logviewer_search_pattern",
        "displayName": "Search WinCC OA Logs",
        "modelDescription": "Search for a specific pattern in log messages. Use when the user wants to find specific text, error codes, or patterns in the logs.",
        "canBeReferencedInPrompt": true,
        "toolReferenceName": "winccoa_search",
        "icon": "$(search-view-icon)",
        "userDescription": "Search for specific patterns in WinCC OA logs",
        "inputSchema": {
          "type": "object",
          "properties": {
            "pattern": {
              "type": "string",
              "description": "Search pattern to find in logs"
            },
            "caseSensitive": {
              "type": "boolean",
              "description": "Whether search should be case-sensitive (default: false)"
            },
            "limit": {
              "type": "number",
              "description": "Maximum number of results (default: 50)"
            }
          },
          "required": ["pattern"]
        }
      },
      {
        "name": "logviewer_get_by_severity",
        "displayName": "Get WinCC OA Logs by Severity",
        "modelDescription": "Get all events for a specific severity level (INFO, WARNING, FATAL, SEVERE, DEBUG). Use when the user asks about all warnings, all errors, or debug messages.",
        "canBeReferencedInPrompt": true,
        "toolReferenceName": "winccoa_severity",
        "icon": "$(list-filter)",
        "userDescription": "Filter WinCC OA logs by severity level",
        "inputSchema": {
          "type": "object",
          "properties": {
            "severity": {
              "type": "string",
              "enum": ["INFO", "WARNING", "FATAL", "SEVERE", "DEBUG", "OTHER"],
              "description": "Severity level to filter by"
            },
            "limit": {
              "type": "number",
              "description": "Maximum number of results (default: 100)"
            }
          },
          "required": ["severity"]
        }
      }
    ],
    "configuration": {
      "title": "WinCC OA LogViewer",
      "properties": {
        "winccoaLogviewer.logLevel": {
          "type": "string",
          "enum": [
            "ERROR",
            "WARN",
            "INFO",
            "DEBUG",
            "TRACE"
          ],
          "default": "INFO",
          "description": "Logging level for extension output",
          "enumDescriptions": [
            "Only critical errors",
            "Errors and warnings",
            "General information, errors and warnings (recommended)",
            "Detailed debugging information",
            "Very verbose trace information (for troubleshooting)"
          ]
        },
        "winccoaLogviewer.logPathSource": {
          "type": "string",
          "enum": [
            "static",
            "workspace",
            "automatic"
          ],
          "default": "automatic",
          "enumDescriptions": [
            "Use a static path configured in settings",
            "Derive path from workspace root (workspace/log)",
            "Automatically detect from selected WinCC OA project (requires Core extension)"
          ],
          "description": "How to determine the log directory path"
        },
        "winccoaLogviewer.staticLogPath": {
          "type": "string",
          "default": "",
          "description": "Static log directory path (used when logPathSource is 'static'). Example: C:\\WinCC_OA\\MyProject\\log or /opt/WinCC_OA/MyProject/log"
        },
        "winccoaLogviewer.background.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable background log monitoring (logs are collected even when LogViewer panel is closed)"
        },
        "winccoaLogviewer.background.maxEvents": {
          "type": "number",
          "default": 500,
          "minimum": 100,
          "maximum": 5000,
          "description": "Maximum number of log events to keep in background buffer (ringbuffer)"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "build": "tsc --build",
    "compile": "npm run build:webview && tsc -p ./",
    "build:webview": "cd webview && npm run build",
    "watch": "tsc --build --watch",
    "lint": "eslint \"src/**/*.ts\"",
    "lint:fix": "eslint \"src/**/*.ts\" --fix",
    "lint:md": "markdownlint \"**/*.md\" --ignore node_modules --ignore webview/node_modules",
    "lint:md:fix": "markdownlint \"**/*.md\" --ignore node_modules --ignore webview/node_modules --fix",
    "format": "prettier --write \"src/**/*.ts\"",
    "format:check": "prettier --check \"src/**/*.ts\"",
    "format:md": "prettier --write \"**/*.md\"",
    "format:md:check": "prettier --check \"**/*.md\"",
    "style-check": "npm run lint && npm run format:check && npm run lint:md && npm run format:md:check",
    "style-fix": "npm run lint:fix && npm run format && npm run lint:md:fix && npm run format:md",
    "test": "npm run style-check && npm run compile && node ./dist/test/runTest.js",
    "test:unit": "npm test",
    "package": "vsce package"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.6",
    "@types/node": "^25.2.2",
    "@types/vscode": "^1.80.0",
    "@typescript-eslint/eslint-plugin": "^8.52.0",
    "@typescript-eslint/parser": "^8.52.0",
    "@vscode/test-electron": "^2.3.9",
    "@vscode/vsce": "^3.7.1",
    "eslint": "^8.57.1",
    "glob": "^13.0.0",
    "markdownlint-cli": "^0.47.0",
    "mocha": "^11.7.5",
    "prettier": "^3.7.4",
    "typescript": "^5.2.0"
  },
  "dependencies": {
    "@winccoa-tools-pack/npm-shared-library-core": "github:winccoa-tools-pack/npm-shared-library-core#main"
  }
}
